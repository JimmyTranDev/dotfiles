<?xml version="1.0" encoding="UTF-8"?>
<settings>
  <pluginGroups>
    <pluginGroup>org.sonarsource.scanner.maven</pluginGroup>
  </pluginGroups>
  <profiles>
    <profile>
      <id>sonar</id>
      <activation>
        <activeByDefault>true</activeByDefault>
      </activation>
      <properties>
        <sonar.sources>.</sonar.sources>
        <sonar.inclusions>src/main/java/**,src/main/resources/**</sonar.inclusions>
        <sonar.exclusions>${code.coverage.exclusions}</sonar.exclusions>
        <sonar.organization>${env.ORG_GITHUB_NAME}</sonar.organization>
        <sonar.host.url>https://sonarcloud.io</sonar.host.url>
      </properties>
      <repositories>
        <repository>
          <id>github-digital</id>
          <name>GitHub ${env.ORG_GITHUB_NAME} maven repo</name>
          <url>https://maven.pkg.github.com/${env.ORG_GITHUB_NAME}/repo</url>
          <snapshots>
            <enabled>true</enabled>
          </snapshots>
        </repository>
      </repositories>
    </profile>
  </profiles>

  <servers>
    <server>
      <id>github-digital</id>
      <username>${env.PRI_GITHUB_USERNAME}</username>
      <password>${env.PRI_GITHUB_TOKEN}</password>
    </server>
  </servers>
</settings>
