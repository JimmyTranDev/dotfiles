# 2026-05-18

## feat(keymaps): add gJ/gK bindings and remove duplicate mappings

- Add gJ for join lines and gK for keyword lookup
- Remove duplicate entries for uv and ug mappings

## feat(snacks): add user-facing text search action with enhanced picker previews

- Add new `text_search.lua` action to search user-facing text in TypeScript/JavaScript files
- Extract strings, template strings, and JSX text while filtering test files and imports
- Add preview support to package.json picker for better UX
- Add preview support to workspace symbols picker (cached and live)
- Add file previews to git hunks picker with proper cursor positioning
